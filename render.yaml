services:
  - type: web
    name: timnet-voicecheck
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: OPENAI_API_KEY
        value: dein-openai-schlüssel
    plan: free